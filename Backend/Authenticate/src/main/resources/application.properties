spring.application.name=Authenticate
server.port=${PORT:8085}

spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost:3306/shipfastAuth?createDatabaseIfNotExist=true}
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:Abcdefg89@}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

jwt.secret=c2hpcGZhc3Qtc2VjcmV0LWtleS1zaGlwZmFzdC1rZXk=
jwt.expiration=3600000

spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:linwan2508@gmail.com}
spring.mail.password=${MAIL_PASSWORD:wwipxuylrsegganx}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS_ENABLE:true}
spring.mail.properties.mail.smtp.starttls.required=${MAIL_SMTP_STARTTLS_REQUIRED:true}
spring.mail.properties.mail.smtp.ssl.enable=${MAIL_SMTP_SSL_ENABLE:false}
spring.mail.properties.mail.smtp.ssl.trust=${MAIL_HOST:smtp.gmail.com}
spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2
spring.mail.properties.mail.debug=${MAIL_DEBUG:false}
spring.mail.properties.mail.smtp.connectiontimeout=${MAIL_CONNECTION_TIMEOUT:120000}
spring.mail.properties.mail.smtp.timeout=${MAIL_TIMEOUT:120000}
spring.mail.properties.mail.smtp.writetimeout=${MAIL_WRITE_TIMEOUT:120000}

# Resend Email Configuration (Set RESEND_API_KEY to use Resend API instead of SMTP)
resend.api.key=${RESEND_API_KEY:}
resend.from.email=${RESEND_FROM_EMAIL:ShipFast <onboarding@resend.dev>}

eureka.client.enabled=false
operations.service.url=${OPERATIONS_SERVICE_URL:https://shipfast-operations-du7o.onrender.com}
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173,https://shipfast-hazel.vercel.app,https://shipfast-new.onrender.com}

# Faster startup
spring.jpa.open-in-view=false
spring.jpa.defer-datasource-initialization=false
spring.main.lazy-initialization=true

spring.main.banner-mode=off
# Reduce hibernate startup time
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

