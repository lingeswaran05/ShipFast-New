server.port=${PORT:8081}

spring.application.name=Shipment-service

spring.data.mongodb.uri=${MONGO_URI:mongodb://localhost:27017/shipment}
operations.service.url=${OPERATIONS_SERVICE_URL:https://shipfast-operations-du7o.onrender.com}
communications.service.url=${COMMUNICATIONS_SERVICE_URL:https://shipfast-communications-rn9l.onrender.com}
auth.service.url=${AUTH_SERVICE_URL:https://shipfast-auth-joke.onrender.com}
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173,https://shipfast-hazel.vercel.app,https://shipfast-new.onrender.com}

spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:linwan2508@gmail.com}
spring.mail.password=${MAIL_PASSWORD:wwipxuylrsegganx}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=${MAIL_HOST:smtp.gmail.com}
spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2
spring.mail.properties.mail.smtp.connectiontimeout=${MAIL_CONNECTION_TIMEOUT:60000}
spring.mail.properties.mail.smtp.timeout=${MAIL_TIMEOUT:60000}
spring.mail.properties.mail.smtp.writetimeout=${MAIL_WRITE_TIMEOUT:60000}

# Resend Email Configuration (Set RESEND_API_KEY to use Resend API instead of SMTP)
resend.api.key=${RESEND_API_KEY:}
resend.from.email=${RESEND_FROM_EMAIL:ShipFast <onboarding@resend.dev>}

spring.main.lazy-initialization=true
spring.main.banner-mode=off
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

